apiVersion: v1
kind: Service
metadata:
  name: aset-whoami
  namespace: aset-whoami
  labels:
    app: aset-whoami
spec:
  type: ClusterIP
  selector:
    app: aset-whoami
  ports:
    - port: 80
      targetPort: 80
