apiVersion: v1
kind: Service
metadata:
  name: php-apache
  namespace: autoscaling-demo
  labels:
    app: php-apache
spec:
  selector:
    app: php-apache
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  type: ClusterIP
