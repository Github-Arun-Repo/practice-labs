apiVersion: v1
kind: Service
metadata:
  name: combined-demo
  namespace: combined-demo
  labels:
    app: combined-demo
spec:
  selector:
    app: combined-demo
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  type: ClusterIP
