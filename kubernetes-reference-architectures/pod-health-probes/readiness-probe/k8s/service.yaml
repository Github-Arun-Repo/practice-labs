apiVersion: v1
kind: Service
metadata:
  name: readiness-demo
  namespace: readiness-demo
  labels:
    app: readiness-demo
spec:
  selector:
    app: readiness-demo
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  type: ClusterIP
